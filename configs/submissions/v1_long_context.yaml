# Profile nộp bài: v1_long_context
# Chỉ dùng nếu log portal báo request bị reject vì context length.

name: v1_long_context
description: Giu baseline nhung tang context window.
image: vllm/vllm-openai:v0.22.1
model_path: /model
served_model_name: Qwen3.5-2B
host: 0.0.0.0
port: 8000
max_model_len: 65536
gpu_memory_utilization: 0.90
tensor_parallel_size: 1
enable_prefix_caching: true
shm_size: 2g
